= simple_form_for @page do |f|
  = f.input :title
  = f.input :menu_order
  = f.input :content, placeholder: 'HAML here will be parse.'
  .form-actions
    = f.submit 'Save', class: 'btn btn-primary'
